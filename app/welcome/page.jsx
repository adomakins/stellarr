
export default function InstallHandler({ searchParams }) {

    // app id is used to customize this page
    const { app } = searchParams;

    return (
        <div>
        <h1>Wait, one more step!</h1>
        </div>
    );

    // enter email to get started, checks within database for the agency

};